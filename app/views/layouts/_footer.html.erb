<footer>
  <small>
    <% if current_user && current_user.admin %> 
      Signed in as <%= current_user.full_name %> (Admin) | <%= link_to "See All Users", admin_users_path %> | <%= link_to "Create a new user", new_admin_user_path %> | (<%= link_to "Log out", session_path("current"), method: :delete %>)
    <% elsif current_user %>
      Signed in as <%= current_user.full_name %> (<%= link_to "Log out", session_path("current"), method: :delete %>)
    <% else %>
      <%= link_to "Log in", new_session_path %> | <%= link_to "Sign up", new_user_path %>
    <% end %>
  </small>
</footer>